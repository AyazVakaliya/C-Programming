#include<stdio.h>
int main()
{
	int a,b,c;
	printf("enter the value of firest latter");
	scanf("%d",&a);
	printf("enter the value of second latter");
	scanf("%d",&b);
	printf("enter the value of third latter");
	scanf("%d",&c);
	if(a>b)
	{
		if(a>c)
		printf("a is maximum");
		
	if(b>c);	
	}
	{
		if(b>a)
		printf("b is maximum");
	}
	 if (c>a)
	{
		if(c>b)
		printf("c is maximum");
	}
	
	return 0;
	
}
